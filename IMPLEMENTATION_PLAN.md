Create functionality to allow users to add their own gliders and glider configuration to the database.
create a button that will allow to preview the gliders polar
Export Tast section on the Task Planner screen is a but too low, extends way over map
When saving waypoint files include some more metadata to the file, like country, allow to display a small preview of the waypoint placement on the map (miniature) before I decide to load it. Do the same or similar to Tasks stored in the database, make sure to adjust the database schema accordingly.
There is no need for a separate "My Files" button or "My Tasks" button, when using browse functionality it is enough that I can toggle "Mine Only"
